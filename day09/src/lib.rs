pub fn interpolate(string: String) -> u32{
    println!("{}", string);
    1
}

struct Oasis {
    active: bool,
    username: String,
    email: String,
    sign_in_count: u64,
}

impl Oasis {
    pub fn eval(&self) -> i64 {
        match &self {
            //Make your recursion here
        }
    }
}